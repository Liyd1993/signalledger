# CockroachDB Cloud connection string. Never commit the real value.
DATABASE_URL=
# AWS deployment settings
AWS_REGION=us-east-1
S3_BUCKET=
BEDROCK_MODEL_ID=
# CockroachDB Cloud Managed MCP Server endpoint, configured as read-only.
COCKROACH_MCP_URL=
